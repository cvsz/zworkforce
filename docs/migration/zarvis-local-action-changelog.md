# Z.A.R.V.I.S. local reversible action slice

- Added loopback-only owner action gateway and isolated local worker.
- Added `sandbox.preference.set` as the only registered reversible capability.
- Added dry-run impact preview, exact SHA-256 approval, one-time nonce, expiry, idempotent execution, compare-and-set drift protection, execution-bound rollback, and emergency revoke.
- Added fixed-path local state and append-only action audit journals.
- Added Owner Action Console, Ubuntu/Linux Compose deployment, automated secret generation, schemas, regression tests, architecture, threat model, runbook, validation record, and CI coverage.

This fragment should be folded into the root `CHANGELOG.md` during release consolidation.
