# CI Baseline

The repository CI validates secret patterns and runs the currently runnable Node and Python test suites.

It does not deploy infrastructure, access production environments, create secrets, or run payment/wallet operations. Add dependency/SBOM, Cloudflare Access, staging, and deployment gates only after operator configuration is approved.
