{
  "name": "@z-platform/zai-factory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:templates": "node validate-manifest.mjs",
    "validate:skills": "node validate-skills-registry.mjs",
    "test": "npm run validate:templates && npm run validate:skills && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
