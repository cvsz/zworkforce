# Clients authenticate to the platform gateway, not directly to a provider.
Z_PLATFORM_AI_GATEWAY_URL=https://api.example.invalid/v1
Z_PLATFORM_SERVICE_TOKEN=replace-with-runtime-secret

# Development-only local gateway endpoint.
# Z_PLATFORM_AI_GATEWAY_URL=http://127.0.0.1:8400/v1
