# ZAI Coder Backend

Python backend package for the Z Platform coding-agent runtime.

This package is installed and tested through repository CI. AI provider access must remain gateway-backed; browser and CLI clients must not receive upstream provider credentials.
