[project]
name = "valixstack"
version = "0.1.0"
description = "Safety-first prediction-market research, backtest, paper, and guarded live framework"
requires-python = ">=3.12"

[project.scripts]
valixstack = "valixstack.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]

