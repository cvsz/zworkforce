# Install as /etc/zworkforce-observability/alert-receiver.env on the
# observability host. Keep this file root-owned; it contains host configuration,
# not credentials. Set the address to the host's private interface used by the
# operator-owned receipt URL and Alertmanager webhook.
ALERT_RECEIVER_BIND=192.168.74.135
